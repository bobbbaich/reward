spring:
  profiles:
    active: test
  datasource:
    url: jdbc:tc:postgresql:14.2:///${random.uuid}
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: ${random.uuid}
    password: ${random.uuid}
logging:
  level:
    com.bobbbaich: DEBUG

security:
  oauth2:
    resource:
      #      userInfoUri: https://cloudbohdan.auth.us-east-1.amazoncognito.com/oauth2/userInfo
      #      tokenInfoUri: https://cloudbohdan.auth.us-east-1.amazoncognito.com/oauth2/token
      jwk:
        key-set-uri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_BSQbCNTKD/.well-known/jwks.json
#    client:
#      clientId: 42jbt1kddgr5ibjj6mtmgobf2l