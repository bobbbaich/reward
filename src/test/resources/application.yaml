spring:
  profiles:
    active: test
  datasource:
    url: jdbc:tc:postgresql:14.2:///${random.uuid}
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: ${random.uuid}
    password: ${random.uuid}
  security:
    oauth2:
      resourceserver:
        stateless: false # https://stackoverflow.com/a/44502136/17200531
logging:
  level:
    com.bobbbaich: DEBUG
    org.springframework.security: DEBUG